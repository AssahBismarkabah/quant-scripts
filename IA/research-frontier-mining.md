# Research Frontier Mining (process)

**Date:** 2026-08-05
**Type:** Process / methodology document (how we discover the NEXT candidate from current academic research, rather than re-testing known/closed buckets)
**Purpose:** Funnel the recent research frontier (last ~6-12 months) into a shortlist of candidates that we can plausibly TEST, applying the market-edge framework's pillars. This replaces "investigate broadly" with "mine the newest evidence for testable mechanics."
**Audience:** self. Informs candidate selection; feeds each candidate's IA research spec + strategy doc.

---

## 1. Why this replaces broad investigation

Every candidate we've tested so far was either an already-published-and-decayed mechanism or a structural flow we measured as too-small / non-persistent. Broad investigation keeps returning to the same weak pool. The frontier-mining approach flips the direction: **start from freshly published academic evidence**, which is:
- less likely to have decayed (our biggest failure mode),
- precisely specified (construction + data + the "why"),
- and gives an explicit funnel from novelty -> testability.

For each paper the funnel surfaces, the gate is unchanged: real forced/mandate counterparty -> data obtainable free/cheap -> enough events -> survives friction. If a fresh paper fails, it bins like the rest — but it starts from new evidence.

---

## 2. The sources (and the accessibility reality)

| Source | Status | How we use it |
|---|---|---|
| **arXiv q-fin** (ST/TR/GN/RM/MF/CP/PM) | **Open API (free)** — category-scoped, clean | **Automated harvest.** No discipline gating needed (q-fin categories). |
| **SSRN** (`papers.ssrn.com`) | No public API; 403-blocks direct/plain-requests; **reached two ways** | (1) **OpenAlex** (indexes SSRN, topic `T10047`) — structured but lagged. (2) **ZenRows** (`ZENROW_API_KEY`) reads **SSRN's own search** (`searchresults.cfm?term=`) with real **Posted dates** — exhaustive + up-to-date. |
| **Crossref** (`api.crossref.org`) | **Open API (free); 429 rate-limits -> retry+backoff** | **Automated harvest**, gated to a finance-journal allowlist (~40 journals) because Crossref has no finance scope. |
| **OpenAlex** (`api.openalex.org`) | **Open API (free, no key)** — occasionally flaky (504/timeouts) | **Automated SSRN/journals coverage**, finance-gated via `primary_topic.id:T10047`. |
| **Google Scholar** (`scholar.google.com`) | No direct API; blocks bots — **reached via SerpApi** (`SERPA_API_KEY`) | **Automated harvest via SerpApi** (keyed, per-query cost). Gated to finance via title/journal filter. |
| **ZenRows (SSRN)** | **Paid anti-bot scraping API** (`ZENROW_API_KEY`); `js_render`+`premium_proxy`=50 credits/call | **Automated SSRN search** — reads SSRN's own results + Posted dates. Finance-gated via title regex. **Exhaustive/up-to-date SSRN.** |
| **ResearchGate** | No machine API; blocks bots (403) | **Manual/curated layer.** Browser-session only. The ONLY remaining manual source. |
| **Semantic Scholar** (`api.semanticscholar.org`) | Hard 429 without an API key; no obvious key-provisioning page | **DROPPED.** No marginal value over OpenAlex/Crossref/Scholar/ZenRows. |

**Honest note on automation:** arXiv exposes a usable API directly. **OpenAlex + Crossref** are the legitimate free route to SSRN/journal content; **SerpApi** automates Google Scholar; **ZenRows** reads SSRN's own search results yourself (with real Posted dates). SSRN's own pages 403 plain requests, so the ZenRows anti-bot route is what fully automates SSRN. Only **ResearchGate** remains browser-only (no machine API of any kind).

Finance discipline gates (native queries have no finance scope, so we gate):
- OpenAlex -> `primary_topic.id:T10047` (Financial Markets and Investment Strategies)
- Crossref -> finance-journal allowlist [~40 names]
- Scholar -> finance title/journal regex filter
- ZenRows-SSRN -> finance title regex (`SSRN_FINANCE`; avoids gas-liquid/tire/lava false positives)
(Only arXiv is intrinsically finance-scoped via its q-fin categories.)

**Manual scan link template (recent)** — for the one remaining manual layer and ad-hoc deep-dives:
- SSRN: `https://papers.ssrn.com/sol3/results.cfm?term=<topic>&date=last_12_months`
- Google Scholar (year-filtered): `https://scholar.google.com/scholar?q=<topic>+anomaly&as_ylo=2025`
- ResearchGate: `https://www.researchgate.net/search/publication?q=<topic>`

---

## 3. The harvest + scorecard (auto: arXiv + Crossref + SSRN-OpenAlex + Scholar-SerpApi + SSRN-ZenRows)

Script: `research/frontier-mining/harvest_frontier.py`. It:
1. Pulls recent q-fin/finance papers from the **arXiv API**, **Crossref**, **SSRN-via-OpenAlex**, **Google Scholar via SerpApi**, and **SSRN via ZenRows** (each finance-gated as described in section 2). Keys read from the repo `.env` (`SERPA_API_KEY`, `ZENROW_API_KEY`).
2. Scores each against the framework's pillars via auditable keyword heuristics:
   - `forced` — mechanism implies a counterparty who MUST trade (rebalance, hedging, dealer, index, buyback, margin/collateral, liquidation, expiry/roll...)
   - `documented` — a measurable mechanic is described (anomaly, return, premium, drift, arbitrage, reversal...)
   - `empirical` — an empirical/findings orientation (predictability, abnormal, event, out-of-sample...)
   - penalized for `method` (pure methods/ML/math — not a tradeable anomaly) and `data_heavy` (needs intraday/secret/proprietary data we can't get)
   - `recent` — within the window.
3. Tags true SSRN rows (`is_ssrn`), outputs a ranked CSV/parquet + a printed shortlist of (empirical, non-method, non-data-heavy) candidates.

Run (full; OpenAlex/Crossref can be flaky/rate-limited — the script retries with backoff; ZenRows consumes credits):
```
.venv/bin/python research/frontier-mining/harvest_frontier.py --max-arxiv 200 --openalex-pages 1 --crossref-rows 50 --scholar-results 30 --zenrows-results 100
```
To skip the credit-consuming ZenRows source, use `--sources arxiv,openalex,crossref,scholar`.
(the venv interpreter is required; `pandas`/`requests` are project deps; Scholar requires `SERPA_API_KEY` in `.env` — already set)

---

## 4. Read-out and decision

- The automated harvest is an **early-warning funnel**, not a verdict. Manual SSRN/Scholar/RG scanning complements it.
- For any surfaced candidate, before a full IA spec: does it clear (a) forced counterparty, (b) free/cheap data + enough events, (c) plausibly non-decayed? If yes, it advances to a full research spec + strategy doc (the buyback-timing treatment).
- If the frontier is method-heavy / thin on high-prior anomalies (as arXiv q-fin appears right now), that is a **honest result** — the funnel said "no strong free candidate is freshly available," which informs a deliberate pick-or-stop rather than forcing a weak idea.

---

## 6. Status

- **2026-08-04:** Process defined + scripted. Accessibility confirmed: arXiv = open API (automated primary); SSRN/Scholar/RG = blocked to scripts (manual layer planned). First harvest (arXiv q-fin, ~12 months, 400 papers) run.
- **2026-08-05:** Multi-source harvest (arXiv + Crossref + SSRN-via-OpenAlex) implemented and **verified end-to-end: 350 papers**. Crossref 429 handled with retry+backoff. **Semantic Scholar dropped** (no key value).
- **2026-08-05b (pipeline correctness fix):** Diagnosed that OpenAlex-SSRN and Crossref had **no finance discipline gate** — they surfaced multidisciplinary noise (concrete, genetics, menopause, ocean, etc.). Fixed by gating OpenAlex to finance topic `T10047` and Crossref to a ~40-journal finance allowlist; also fixed an OpenAlex bug that had been returning the *title* as the abstract. Re-run: noise gone, output finance-only.
- **2026-08-05c (Google Scholar added):** User provisioned a **SerpApi** key (`SERPA_API_KEY` in `.env`). Added `fetch_scholar_serpapi` — automated **Google Scholar** coverage, finance-gated via title/journal filter. Verified: returns high-quality papers other sources miss (e.g. "Business-Cycle Risk Exposure and the Cross-Sectional Returns in China's A-Share" — Emerging Markets Finance and Trade; "Market Structure and the Erosion of Informed Trade" — Journal of Portfolio Management; SSRN working papers). ResearchGate remains the only manual-only source.
- **2026-08-05d (SSRN coverage gap found + fixed):** The OpenAlex SSRN branch was under-covering recent output — OpenAlex indexes only ~25 finance-topic SSRN articles in the last 12 months (a lagging, partial subset of SSRN's real volume). Added **SSRN-scoped SerpApi Scholar queries** (`site:papers.ssrn.com ...`) so recent SSRN finance papers OpenAlex misses are recovered **via the licensed API (no SSRN scraping)**. Verified: now surfaces fresh SSRN papers e.g. "Passive Flows, Index Rebalancing, and Price Impact" (SSRN 5703304), "Explaining Anomalies" (Hollstein, Kowalke), "What Drives Anomaly Premia around the World?".
  - A direct SSRN scraper (`research/frontier-mining/scraper.py`, mass-scrape by abstract_id) exists in the repo but is **NOT run/recommended**: it is ToS-violating, unfiltered by discipline, and 403-flaky. SerpApi-SSRN does the same job legally.
- **2026-08-05e (SSRN gate):** Added a definitive **`is_ssrn`** detection (URL `papers.ssrn.com/sol3/papers.cfm?abstract_id=` OR DOI `10.2139/ssrn`) so true SSRN working papers are cleanly separated from published/publisher-paper versions that OpenAlex mis-tags as SSRN.
- **2026-08-05f (SSRN complete — ZenRows):** User provisioned a **ZenRows** key (`ZENROW_API_KEY` in `.env`). Added `fetch_ssrn_zenrows` which reads **SSRN's own search UI** (`searchresults.cfm?term=<finance query>`) via ZenRows (`js_render` + `premium_proxy`, 50x credits/call), parsing titles, abstract_id URLs and real **Posted dates**. This closes even the last **exhaustive/up-to-date SSRN** gap (SSRN has no API and 403-blocks direct/plain-requests; ZenRows is the anti-bot route). Finance-gated via a tight `SSRN_FINANCE` title regex (avoids gas-liquid/tire/lava/convection false positives). `ssrn(zenrows)` is now a 5th automated source; **no manual SSRN browse needed** for mining.
- **Cost note:** ZenRows `js_render`(5x)+`premium_proxy`(10x)=**50 credits/call**; reads ~20-50 SSRN papers per finance query. A full multi-query run is a handful of calls — small on a 5000-credit plan.
- **Upstream reliability note:** OpenAlex and Crossref are free APIs that intermittently 504/429 and time out; the script retries with backoff and connection-error handling. When they are flaky, arXiv + Scholar + SSRN(zenrows) carry the harvest.
- **Sources now (5 automated, all finance-gated):** arXiv q-fin (open API) · Crossref (finance-journal allowlist) · SSRN-via-OpenAlex (topic T10047) · Google Scholar via SerpApi (title/journal filter) · **SSRN via ZenRows (SSRN's own search + Posted dates)**. **ResearchGate is the only remaining manual-only source** (no machine API of any kind).
- **Verified outputs:** `research/frontier-mining/outputs/frontier_papers.csv` / `.parquet` (scored/ranked).
- **First-run honest result:** recent machine-accessible frontier is **method-heavy and thin on high-prior, free-data-testable structural anomalies**; the funnel's job is to surface the few fresh, testable theses from the noise (e.g. "AI and Exchange Rate Predictability" Sharpe>0.7; JFE "Index rebalancing ... Do indexes time the market?").
- Next: (a) pick a surfaced thesis, (b) assess whether it makes sense + is testable with free data, (c) decide advance-to-spec or bin, (d) if nothing clears, deliberately stop/regroup.
- **2026-08-07 (triage — HK passive-flow candidate CLOSED):** First candidate pulled from the CSV: Xu, "Passive Flows, Index Rebalancing, and Price Impact: Evidence from a Quasi-Experiment in Hong Kong" (SSRN 5703304, score 9.0). Note: surfaced twice in the CSV (once via `ssrn(zenrows)` with real posted date 2025-11-10, once via `scholar(serpapi)`) — a dedup wrinkle to watch. Full-text review: genuine, well-identified effect (Hang Seng weight-cap quarterly rebalancing; IV elasticity of demand ≈ -0.25, more inelastic than US mega-caps; -0.6% rebalance-day AR; two clean placebos). **But the paper's own Section 6.2 friction backtest closes it for us:** the long-short trade's annualized Sharpe falls **1.10 (frictionless) → 0.46 (+HK stamp duty 0.13% + broker) → 0.29 (+impact) → 0.28 (+borrow)**. HK stamp duty is charged on both legs, structurally unfavorable vs our single-leg US short-additions line; effect is concentrated in ~4-5 mega-caps (Tencent/Alibaba/HSBC/AIA) with tiny N (32 binding events, ~192 obs), plus capacity capped by ~$36B USD HSI-linked AUM. We cannot beat the paper's friction assumptions, so the line is **closed as not viable post-friction** — no new HK data/event-study line opened. Recorded here so it is not re-surfaced repeatedly.
- **2026-08-07b (triage — VIX-ETP end-of-day hedging candidate REGISTERED, not advanced):** Second candidate: Bangsgaard & Kokholm, "The Stock Market Impact of Volatility Hedging: Evidence from End-of-Day Trading by VIX ETPs" (JBF 2025, score 9.0; also appears as a duplicate crossref row — flagged in the dedup note). Mechanism: VIX ETP issuers rebalance VIX-futures near the 16:15 close; VIX-futures market makers hedge by selling SPX futures → a mechanical EOD SPX-futures price impact. Strong gross signal (EOD 16:05-16:15 return R2 4.6% in / 4.0% OOS; naive long/short Sharpe **1.47** gross). **But the paper's own Appendix B kills the naive trade:** one tick of SPX-futures spread (0.25pt) makes every naive variant NEGATIVE (Sharpe **-0.56**). Only a cost-threshold + model-forecast variant survives (+1.05%/yr, Sharpe 0.91) — and that needs Bloomberg VIX-ETP AUM/flows + intraday SPX/VIX-futures tick data (TickData 1-min), plus the reversal leg itself is only weakly supported (baseline reversal regression insignificant; only asymmetric/non-linear specs show it). **Initial verdict: REGISTERED, treated as a heavy Level-2 microstructure build (intraday tick + Bloomberg-VIX-ETP-flow data), NOT a cheap free-data reversal. Not advanced now. [Now superseded by the firm CLOSED in 2026-08-11c — full-paper review confirmed it fails on BOTH friction and data gates.]** Same takeaway as HK: the gross alpha is real but is consumed by realistic costs unless you add a costly, parameter-sensitive execution layer.
- Next (superseded by 08-07c/d/e): the 9.0/8.0 remaining candidates have all now been triaged — Same Dollar (08-07c), AI-FX (08-07d), and the rest of the 9.0/8.0 tier (08-07e). None clears the bar. Per section 4, the honest move is a deliberate pick-or-stop: re-run the harvest later for fresh material, or pick a different (non-frontier) line, rather than force a weak candidate.
- **2026-08-07c (triage — "Same Dollar, Different Impact" candidate REJECTED, not a tradable line):** Huang, Hu, Song & Xiang, "Same Dollar, Different Impact: Investor Flows Are Not Equally Price-Moving" (SSRN 6266398, score 9.0). Full-text triage. Finding: mutual-fund flows generate real price impact / fire sales (-2.27% CAR) / fragility, but comparable separate-account (institutional) flows generate essentially zero price impact, even within same-manager/same-strategy twins and across liquidity regimes; mechanism is institutional execution design (transition managers absorb demand at ~4-11 bps vs mutual-fund 47-94 bps). **Verdict: no tradable signal.** There is nothing to capture/minimize — the paper's contribution is that separate-account flows are *benign*, which is not a return source; it also *reduces* the prior on trades that rely on institutional-flow price pressure. Data wall (Morningstar separate-account holdings + proprietary transition-manager trade data), no forced counterparty, no exploitable edge. Recorded as literature/reasoning (demand transmission, 13F-multiplier caveat), not a candidate line.
- **2026-08-07d (triage — "AI and Exchange Rate Predictability" candidate REGISTERED, not advanced):** Izadyar, "AI and Exchange Rate Predictability" (arXiv 2608.00761, score 9.0), Imperial College London, Aug 2026. Full-text triage. Mechanism: LLM (GPT-4o / DeepSeek-V3) classifies each macro economic data release (Investing.com calendar: realized vs previous vs consensus forecast, ~174,820 points 1996-2024, G-10) as STRENGTHEN/WEAKEN/neutral; AIFX index = net positive-minus-negative news over a lookback; cross-sectional rank, go long top-2 / short bottom-2, monthly, USD-neutral. Gross performance: Sharpe rises with lookback, ~0.58-0.60 for 36-48mo (abstract's ">0.7" is the best window in Fig 1; Table 3: 0.577/0.604/0.594/0.491/0.467 for 36/42/48/54/60mo); alpha ~2.9-3.2%/yr (36-48mo) net of Dollar/Carry/Momentum/Value, NYW-sig at 5%; robust to DeepSeek, weighted, time-series. **Friction: passes easily** — single G-10 spot/forward instrument, one tick of spread is negligible vs ~13% annualized; only FX spot/1M forwards needed. **Forced counterparty: WEAK** — not a discrete mechanical forced flow; mechanism is gradual/underreaction to *positive* fundamental news (negative news passes and re-prises quickly, positive drifts), i.e. the counterparty is the marginal spot taker who fails to price cross-sectional fundamental drift. Tradable (a genuine cross-sectional rank delivered via spot), unlike Same Dollar, but no identifiable single forced counterparty. **Non-decayed: the red flag** — the entirely new twist (running ChatGPT/DeepSeek on structured release data) is trivially replicable by anyone with an LLM subscription, and AI-driven FX arbs are precisely the edge that decays/crowds as AI adoption spreads post-2023; the paper's sample **ends at GPT-4o's knowledge cutoff (Oct 2023), so the whole post-cutoff window is untested OOS**. **Data: free** (Investing.com calendar is freely accessible; FX spot/forwards cheap). **Verdict: REGISTERED, not advanced now** — the cleanest friction/free-data profile of any candidate so far, but weak on forced-counterparty and carrying high decay/crowding + untested-OOS risk. Candidate worth revisiting only if we wanted to run a costly, current-GPT live replication on post-2023 data to test whether the drift survives the AI era. **Model-variant caveat (recorded so it isn't lost if we revisit):** the paper's backtest is tied to the specific model it used (GPT-4o primary, DeepSeek-V3 replicate, GPT-3.5 for the cutoff DiD test) — the directional analysis and therefore the AIFX signal are **model-dependent**, so a newer/different model is a *different variant* whose claim to edge must be justified by **its own backtest** (re-run the full protocol with that model over the same data, then hold out post-cutoff OOS), not by extrapolating GPT-4o's Sharpe. We are NOT testing this now (2026-08-07); if the AI-FX line is ever re-opened, it is only the current-model rolling replication that can address the newer-model + untested-OOS questions, and it must stand on its own evidence.
- **2026-08-07e (triage — remaining 9.0/8.0 candidates binned in one pass, so they are not re-surfaced):** The AI-FX line is closed for now (model-dependent, needs its own backtest). The rest of the 9.0 tier does not clear the bar and is recorded here so we stop looping on it:
  - **"The hidden costs of hedge fund activism: insights into market liquidity dynamics"** (Meles et al., European Journal of Finance, 9.0) — **REJECTED, no tradable signal.** Descriptive DiD finding that hedge-fund activism *deteriorates* stock liquidity (worse with information asymmetry/financial constraints). Same class as Same Dollar: an effect on liquidity, not a return source with a forced counterparty we can capture; no exploitable edge, proprietary event data.
  - **"How Likely and How Deep? Sharp Joint Bounds on Risk-Neutral Crash Probability and Conditional Depth from Option Bid-Ask Quotes"** (arXiv 2607.25353, 9.0) — **REJECTED, method (out of scope).** A sharp-bounds/robust-hedging method on option quotes; it tightens a bounds computation, not a tradeable anomaly. Method-heavy, no forced counterparty.
  - **"The Trillion Dollar Bonus of Private Capital Fund Managers"** (Phalippou, Management Science, 9.0) — **REJECTED, descriptive (out of scope).** Measures aggregate carried interest; compensation accounting, no tradable signal.
  - **"Deep Hedging Under Market Frictions: A Comparison of DRL Models..."** (JRFM 2025, 9.0) — **REJECTED, method (out of scope).** Deep-reinforcement-learning options-hedging methodology; a methods comparison, not a tradeable anomaly. (Recorded here so we don't re-open it as a "candidate".)
  - DeFi/ML/theory arXiv 9.0s (DeTEcT token economies, ZAPs DeFi reward attribution, green PPA hedging, rough volatility pathwise filtering, China A-share herding ABM, VAIOM FX sequence ML, abliteration model-safety, binary-outcomes simulation, order-book herding ABM) — **all method/ML/out-of-scope** (no forced-cash-flow counterparty, no free-data tradeable edge); not candidates.
  - 8.0 tier: "When Retail Investors Strike" (momentum crashes/reversals, CSMAR A-share **proprietary money-flow data**, recent=False) and "A Tale of Two Anomalies" (value/momentum re-examination, recent=False) — **REJECTED/not advanced** (known-anomaly buckets + data wall + not recent). "Predatory Trading Effects ... Index Rebalancing" (2020, recent=False) — index-rebalancing is a **closed bucket** for us (US small/mid CLOSED 2026-08-04). Older hardcopy SSRN 7-8.0s (Return Volatility/Trading Volume; Cash Flow Volatility/Systematic Risk) are canonical cross-refs, recent=False — not candidates.
  - **Result:** every empirical (non-method, non-data-heavy) 9.0/8.0 frontier candidate has now been triaged. None clears the full bar (forced counterparty + free data + non-decayed + survives friction). Consistent with the process doc's honest-funnel guidance in section 4: there is no strong freshly-available free-data candidate right now → deliberate pick-or-stop rather than forcing a weak idea.
- **2026-08-07f (triage — RGRR QQQ-DIA rotation candidate REJECTED, out of framework):** Re-surfaced by the 2026-08-07 re-harvest (the one genuinely new empirical candidate; score 9.0). Xiong, "Relief-Gated Relative Rotation for QQQ-DIA Allocation" (arXiv 2607.06117, Jul 2026), with a public **GitHub replication package**. Full-text triage. Mechanism: a two-ETF timing rule that continuously maps screened relative (QQQ/DIA momentum, reversal, drawdown) + macro/stress states (rate relief, VIX, HYG/SHY credit, SPY drawdown) into a QQQ weight (0-100%, DIA the residual; no cash/leverage). Fixed, globally-HAC-screened signal universe (1 main effect + 9 2nd-order + 2 3rd-order interactions) with walk-forward rolling re-selection of only the lambda weights; 10bp one-way cost. Results (OOS): improves Sharpe vs 100% QQQ in every window (2018: 0.94 vs 0.89; 2022: 0.87 vs 0.70) but **trails QQQ on CAGR in every window except 2022** (2018: 18.33% vs 20.50%; 2020: 18.69% vs 21.29%) — i.e. it beats QQQ only on risk-adjusted terms by shaving drawdown, and loses to buy-and-hold QQQ in terminal wealth in 3 of 4 windows. Beats 50/50 QQQ-DIA on all three dims every window. **Verdict against our framework: fails the core premise — there is NO forced/mandate counterparty.** The edge is a discretionary market-timing call on QQQ-vs-DIA relative return against other allocators (a zero-sum timing bet), not a structural-flow price pressure being front-run. **Free data: passes** (QQQ/DIA/yields/VIX/credit all free; public code). **Friction: partial** — spreads are tiny (1-2bp), but turnover is 354-506%/yr (the paper's own stated main weakness), so all-in costs erode the slim Sharpe surplus; also high tax drag on churn. **Non-decayed: poor** — the raw states (relative reversal + rate/growth timing) are decades-old crowd-arbed timing factors and the exact interaction stack is a **published preprint with public code**, so it self-crowds immediately. Bottom line: not a forced-counterparty line at all (it is a market-timing rule, which our framework does not buy), published+open so decaying, does not dominate its own benchmark on return — **REJECTED, out of framework.**
- **2026-08-07g (triage — "Option Market Making with Hedging-Induced Market Impact" candidate REJECTED, method / out of framework):** Aubert, Chevalier & Ly Vath (Applied Mathematical Finance, 2026, score 9.0). Full-text triage. This is a **stochastic-control / market-making execution model**: it models an option market-maker whose hedging trades cause both permanent and transient price impact on the underlying (Hawkes/compounded-Poisson flow, coupled option-underlying dynamics), proves well-posedness (no instantaneous/dynamic/round-trip manipulation, terminal no-arbitrage bound), and solves the quoting/hedging problem with a **deep-hedging-style neural policy** trained on a **synthetic simulator** (parameters chosen, P=100, 5-week horizon, BS reference price — not real data). Results are simulator P&L in arbitrary units where the learned policy merely *loses less* than a naive benchmark (-232 vs -263 at I0=-100; +55.4 vs +55.7 at I0=0); several configurations are outright negative. **Verdict: same class as Deep Hedging (which is binned in 08-07e) — REJECTED, method (stochastic control / DRL), out of framework.** No real-data empirical result, no forced/mandate counterparty to front-run, no tradable anomaly (it's about *being* the market maker and optimizing behavior, an execution/liquidity-provision model, not a return source). No signal survives to test.
- **Pattern note (all frontier candidates, finalized):** HK = real effect, killed by friction (stamp duty). VIX-ETP = gross Sharpe 1.47 but naive trade killed by 1 tick; only heavy L2 cost-threshold build survives. Same Dollar = no tradable signal (effect is "no effect"). AI-FX = passes friction + free data easily (best profile yet) but weak forced-counterparty and heavy decay/crowding risk; model-dependent. RGRR = a market-timing rotation rule **with no forced counterparty at all** (fails the framework's core premise), published+open (self-crowding), and only beats QQQ on Sharpe not return. Option MM w/hedging impact (08-07g) = stochastic-control/DRL market-making model with **no real-data empirical result and no forced counterparty** — same bin as Deep Hedging. The funnel consistently surfaces either (a) real forced-counterparty effects whose executable layer is friction-eaten (HK, VIX-ETP), (b) descriptive results with no signal (Same Dollar, activism), (c) method/theory/ML/control out-of-scope (Deep Hedging, option-MM), or (d) discretionary timing rules that fail the forced-counterparty pillar (RGRR) — consistent with prior findings that free-data, high-prior, non-decayed, forced-counterparty trades are scarce. **No untriaged empirical 9.0/8.0 frontier candidate remains after the 2026-08-07 re-harvest either.**
- **Dedup wrinkle found (pipeline note):** the CSV can carry the same paper twice across sources with different scores — "Passive Flows" (SSRN 5703304) appears via both `ssrn(zenrows)` (9.0) and `scholar(serpapi)` (8.0); the JBF 2025 VIX-ETP paper appears as two identical `crossref` rows. A post-harvest dedup on normalized title (or URL/abstract_id) would collapse these before ranking. Not yet implemented.
- **2026-08-11a (arXiv outage + pipeline reliability fix):** Full re-harvest stalled because `export.arxiv.org` was intermittently down. Diagnosis: it was **not** our IP — a single-category curl returned HTTP 200 in ~0.5s, while the code's single massive query (7 q-fin categories OR'd, 200 results) tripped arXiv's rate limiter (429 → 503 → ReadTimeout). Fixed in `harvest_frontier.py`: split `fetch_arxiv` into **one-request-per-category** (small queries pass arXiv's filter) and added `_arxiv_get()` — a retry helper honoring `Retry-After` on 429/503 and retrying on timeout/connection errors with growing backoff. Result: arXiv branch recovered, fetched **196 papers**. Merged into the main file → final **478 papers / 285 empirical** across all 5 sources (`outputs/frontier_papers.csv`).
- **2026-08-11b (triage — newly-surfaced arXiv 9.0s from the merge, all binned):** the arXiv batch carried over ~10 scorers at 9.0 that are not previously recorded. Triage:
  - **"On a Simple Relationship Between Order Imbalance, Skew and Width in OTC Trading"** (arXiv 2608.07690, age 4d) — **REJECTED, microstructure theory (same bin as Option-MM/Hawkes-OTC).** Models a market maker absorbing imbalanced RFQ flow via skew/width translation; it's a quoting-behavior/market-making model, **no forced-counterparty return anomaly to front-run, no real-data empirical result** (steady-state symmetry theorem). Best candidate otherwise; still out of framework.
  - **"How Likely and How Deep? Sharp Joint Bounds on Risk-Neutral Crash Probability..."** (arXiv 2607.25353) — **REJECTED, method** (bounds computation; already noted 08-07e; re-confirmed).
  - **"Herding and Liquidity in Order-Book Markets. II. Fundamental Anchoring..."** and **"Herding, Momentum, and Reversal in China's A-Share ... ABM"** — **REJECTED, ABM/method** (agent-based simulation, no real forced-cash-flow counterparty, no free-data edge).
  - The rest of the arXiv 9.0 tier (DeFi ZAPs/DeTEcT, green PPA hedging, AI-FX dup, tax-migration tipping-point, correlated-binary simulation, abliteration) — **REJECTED**, DeFi/ML/sim/out-of-scope (all already-in-class; no new candidates).
  - **Verdict:** consistent with the 08-07 pattern — the recurring arXiv q-fin frontier is method/ABM/ML-heavy; **no untriaged empirical structural-edge candidate remains from the 478-paper merged harvest.** The funnel's honest output is still: no strong freshly-available, free-data, forced-counterparty edge → deliberate pick-or-stop (nothing new advances to a full IA spec).
  - Google Scholar / SSRN(zenrows) / Crossref 9.0s in this merge are the **already-triaged** set (VIX-ETP CLOSED now — see 2026-08-11c; HK CLOSED; Same Dollar/AI-FX/activism/Deep Hedging/Option-MM as recorded) — no new rows advanced.
- **2026-08-11c (triage — VIX-ETP line CLOSED outright, full-paper confirmation; supersedes 08-07b "REGISTERED, not advanced"):** Full text of Bangsgaard & Kokholm, JBF 180 (2025) 107556 reviewed. Confirms the mechanism is genuinely structural/forced (VIX ETPs mechanically rebalance VX1/VX2 near the 16:15 close; VIX-futures market makers hedge by **selling SPX futures** → ex-ante-quantifiable EOD SPX price pressure; OOS R² 4.0–7.2%, asymmetric on the buy-signal side, t=−9.8; robust to controls incl. SPX/VIX-option gamma and info-flow decomposition). **But it fails our testability gates on BOTH friction and data, so it is CLOSED — not worth a full IA spec.**
  - **Friction gate fails (the paper's own Appendix B):** naive signal strategy `s(D)` = gross Sharpe **1.47**, but after **a single tick** of the SPX-futures bid-ask spread ($12.5 = 0.25pt) **every naive variant goes negative** (Sharpe **−0.56**). Only a cost-threshold + expanding-window regression-forecast version survives at a marginal **Sharpe 0.91** (+1.05%/yr) vs 0.60 for the frictionless benchmark — a thin, execution-parameter-sensitive edge, not a robust one.
  - **Data gate fails (no free/cheap path):** the signal `D_ETP` needs **daily VIX-ETP AUM per ETP measured at 16:00** (paper uses Bloomberg; authors "do not have permission to share data") + intraday tick data for VX1/VX2/ES (TickData) + option net-gamma controls (OptionMetrics). Reconstructing historical daily AUM across all short-term VIX ETPs cleanly is not feasible free/cheap.
  - **Verdict: CLOSED.** Same pattern as HK stamp-duty — a real forced-counterparty mechanism whose executable layer is friction-eaten and data-heavy. Recorded so it is not re-surfaced/re-litigated.
- **Frontier status (final after 2026-08-11c):** every empirical (non-method, non-data-heavy) 9.0/8.0 candidate across the full 478-paper, 5-source merged harvest has now been triaged. **None clears the full bar** (forced counterparty + free/cheap data + enough events + survives friction + non-decayed). The funnel's honest, repeatable output is: no strong freshly-available, free-data, forced-counterparty edge is currently on offer. Standing policy: deliberate **pick-or-stop** — do not force a weak candidate; re-run the harvest on fresh material or pick a deliberate non-frontier line.
- **2026-08-11d (off-frontier follow-up — Bitcoin Cycle × MVRV-Z band confluence CLOSED):** Separately, we tested whether combining the "Bitcoin Cycle" indicator (1Y-MA×2 vs 116D tops; 232D vs 2Y-MA bottoms) with the MVRV-Z low/high rescaled band helps time BTC. Result: the cycle indicator fires only **4 tops / 4 bottoms in 16y**, so the strict confluence fires **2 trades total (N=2) — not testable**; relaxed variants (MVRV band alone, either-signal) neither beat buy-and-hold on return nor add value over MVRV-Z alone, and long-short is unstable (DD −129%). Same conclusion as the standalone MVRV DISCONFIRMED probe. Recorded in `../research-specs/bitcoin-mvrv-research-spec.md` §8. Not a candidate line.

